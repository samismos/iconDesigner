#pragma once
#include <vector>
#include <string>
using namespace std;

void print_row_length_color(int times, string color);
void initializeMenu();
void print_string_with_background_color(string color, string text);
void displayGrid(vector<vector<string>>& grid, bool isNew);
